apply plugin: 'idea'

subprojects {
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    version = '1.0'
    sourceCompatibility = 1.6

    jar {
        manifest.attributes provider: 'gradle'
    }
}